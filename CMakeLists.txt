cmake_minimum_required(VERSION 3.5)
set(EXTRA_COMPONENT_DIRS ${EXTRA_COMPONENT_DIRS} components/network)
set(EXTRA_COMPONENT_DIRS ${EXTRA_COMPONENT_DIRS} components/http)
set(EXTRA_COMPONENT_DIRS ${EXTRA_COMPONENT_DIRS} components/ota)
set(EXTRA_COMPONENT_DIRS ${EXTRA_COMPONENT_DIRS} components/m_mqtt)
set(EXTRA_COMPONENT_DIRS ${EXTRA_COMPONENT_DIRS} components/m_esp_now)
set(EXTRA_COMPONENT_DIRS ${EXTRA_COMPONENT_DIRS} components/control_driver)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(E_lable)
